{{- if .Values.enabled }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.name }}-svc
spec:
  selector:
    app: {{ .Values.name }}
  ports:
  - port: 8080
    targetPort: 8080
    nodePort: {{ .Values.service.nodePort }}
  type: {{ .Values.service.type }}
{{- end }}