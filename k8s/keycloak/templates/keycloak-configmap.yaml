{{- if .Values.enabled }}
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Values.name }}-realm-config
  labels:
    app: {{ .Values.name }}
data:
  realm-config.json: |
{{ $.Files.Get "configs/keycloak-realm.json" | indent 4 }}
{{- end }}